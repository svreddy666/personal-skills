Contributing

- For personal use: commit directly to main.
- For collaborative use: open a branch and submit a PR describing the skill and how it triggers.
- One skill per folder: `skills/<kebab-name>/SKILL.md` with `name` + `description` frontmatter.
- Keep each `SKILL.md` focused and actionable; move long templates/examples into `references/`.
